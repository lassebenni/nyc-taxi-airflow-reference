# Packages injected into the class VM's Airflow image at container start
# via _PIP_ADDITIONAL_REQUIREMENTS (see datatrack/bicep/modules/vm-airflow.bicep).
# Keep this list in sync with the airflowPipRequirements default in the bicep.
#
# DO NOT add dbt here: dbt-core==1.10.* conflicts with Airflow 2.9's OTEL pins
# and causes pip to backtrack indefinitely on every container restart. Students
# whose DAGs need dbt should install it inline in the BashOperator that runs it:
#   bash_command="pip install --quiet dbt-postgres==1.10.* && dbt run --project-dir ..."
# Or bake a custom Airflow image via a Dockerfile (future work).
apache-airflow-providers-postgres
apache-airflow-providers-microsoft-azure
psycopg2-binary
pyarrow
pandas
requests
